import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["avatar.iran.liara.run","images.unsplash.com","images.pexels.com","sjgmhm4pqb.ufs.sh"], // Add the external domain here
  },
};

export default nextConfig;
